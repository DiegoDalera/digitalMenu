/* eslint-disable no-unused-vars */
/* eslint-disable react/prop-types */

function Cart({ orderCount, onAddToOrder }) {
  return (
    <div className="cart">
      🛒 {orderCount}
    </div>
  );
}

export default Cart;
